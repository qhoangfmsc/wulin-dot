/** Quality tier rolled onto an item at summon time — see `data.ts` for the
 * display name/color/stat multiplier of each, and `store.ts` for how the
 * odds shift with the summon store's level. */
export type Rarity = "common" | "rare" | "epic" | "legendary";
