/** English values in code, Vietnamese only at display time (see
 * `data.ts`'s `UNLOCK_FEATURE_NAMES`) — literal union, extend as new gated
 * features get added. */
export type UnlockableFeature = "summonStore" | "market";
