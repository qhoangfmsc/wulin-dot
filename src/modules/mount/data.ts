import type { MountConfig, MountId } from "./types";

/** No mount is real yet — empty on purpose. Mounts are for faster movement,
 * DIFFERENT from `modules/pet/` (a companion, not something you ride). Add
 * an entry here once there's a real mount to show. */
export const MOUNTS: Record<MountId, MountConfig> = {};
