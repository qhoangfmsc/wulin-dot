import type { PetConfig, PetId } from "./types";

/** No pet is real yet — empty on purpose. Add an entry here (same shape as
 * `modules/character/data.ts`'s `CHARACTERS`) once there's a pet to show. */
export const PETS: Record<PetId, PetConfig> = {};
